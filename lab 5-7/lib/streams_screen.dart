import 'package:flutter/material.dart';

class StreamsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Streams'),
      ),
      body: Center(
        child: Text('Streams Screen'),
      ),
    );
  }
}
